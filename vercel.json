{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "regions": ["iad1", "bom1"],
  "crons": [
    {
      "path": "/api/data/refresh",
      "schedule": "*/30 * * * *"
    }
  ]
}
